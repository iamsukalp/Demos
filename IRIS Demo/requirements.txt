# IRIS IVR Demo - Python Dependencies
#
# Core server uses Python stdlib (http.server, socketserver, json, asyncio, threading).
# The websockets package is required for the AI voice mode (OpenAI Realtime API relay).

websockets>=12.0
