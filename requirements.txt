# EXL AI Demo Suite - Python Dependencies
#
# Core server uses only Python stdlib (http.server, socketserver, json, asyncio, threading).
# The websockets package is required for the Luna IVR AI voice mode (OpenAI Realtime API relay).
#
# Front-end dependencies (Tailwind CSS, Chart.js, Marked.js) are loaded via CDN at runtime.

websockets>=12.0
